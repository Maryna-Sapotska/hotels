# Property View - Hotel Management API

## Описание
Проект представляет REST API для управления отелями, поиска по параметрам и построения статистики (гистограммы).  
Используются Spring Boot, Spring Data JPA, Liquibase, H2 (в памяти) и Swagger/OpenAPI для документации.

## Функционал
- CRUD для отелей
- Добавление удобств к отелям
- Поиск отелей по имени, бренду, городу, стране и удобствам
- Получение гистограмм по бренду, городу, стране и удобствам

## Использованные паттерны
- **DTO** – для передачи данных между слоями и клиентом
- **Mapper / Adapter** – `HotelMapper` для преобразования сущностей
- **Service Layer** – `HotelService` для бизнес-логики
- **Repository / DAO** – `HotelRepository` для работы с БД
- **Specification Pattern** – `HotelSpecification` для динамических фильтров
- **MVC (Controller)** – REST-контроллеры для обработки запросов
- **Builder / Fluent API** – для тестов и создания объектов

## Технологии
- Java 21
- Spring Boot 4
- Spring Data JPA / Hibernate
- Liquibase
- H2 Database (по умолчанию)
- JUnit 5 + Mockito для тестов
- Swagger/OpenAPI (`springdoc-openapi`)

## Настройка БД
По умолчанию используется H2 in-memory.  
Для смены БД:

1. Изменить параметры в `application.yml`:
```yaml
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/hotelsdb
    username: postgres
    password: password
    driverClassName: org.postgresql.Driver
```
2. Добавить соответствующий драйвер в pom.xml:
```xml
   <dependency>
        <groupId>org.postgresql</groupId>
        <artifactId>postgresql</artifactId>
        <version>42.6.0</version>
   </dependency>
```
3. Liquibase создаст таблицы автоматически при старте приложения.

## Запуск приложения
mvn spring-boot:run

## API доступно по контексту 
/property-view

## Swagger UI: 
http://localhost:8092/property-view/swagger-ui/index.html

## H2 консоль: 
http://localhost:8092/property-view/h2-console

## Тесты
- Интеграционные тесты для контроллеров и сервисов
- Используется MockMvc для REST тестов
- Mockito для юнит-тестов сервисного слоя

Запуск тестов:
mvn test

## Примеры эндпоинтов

- GET /hotels – список всех отелей
- GET /hotels/{id} – детальная информация об отеле
- POST /hotels – создание отеля
- POST /hotels/{id}/amenities – добавить удобства
- GET /search – поиск отелей
- GET /histogram/{param} – гистограмма (brand, city, country, amenities)

## 📧 Контакты

Если у вас есть вопросы, пишите:
📬 sopotkom@mail.ru